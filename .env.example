# Database Configuration
# MySQL connection string - Required for database operations
# Format: mysql://username:password@host:port/database
DATABASE_URL="mysql://username:password@localhost:3306/events_studio"

# Example for PlanetScale (recommended for production):
# DATABASE_URL="mysql://xxxxxxxxxx:pscale_pw_xxxx@aws.connect.psdb.cloud/events_studio?ssl={"rejectUnauthorized":true}"

# Authentication & Security
# Secret key for JWT token signing - Generate a random string
JWT_SECRET="your-super-secret-jwt-key-change-this"

# Manus OAuth Configuration
VITE_APP_ID="your-app-id-here"
OAUTH_SERVER_URL="https://oauth.example.com"
OWNER_OPEN_ID="your-owner-openid-here"

# Environment
NODE_ENV="development"  # or "production"

# API Keys (Optional - for AI features)
BUILT_IN_FORGE_API_URL="https://api.example.com"
BUILT_IN_FORGE_API_KEY="your-api-key-here"

# Server Configuration
PORT=3000

# Notes:
# 1. Copy this file to .env and fill in actual values
# 2. Never commit .env to version control
# 3. Generate strong random strings for JWT_SECRET (minimum 32 characters)
# 4. For JWT_SECRET, you can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
